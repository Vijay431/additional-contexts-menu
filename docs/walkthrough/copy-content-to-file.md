# Copy Content to File

Select any block of code in a TypeScript or JavaScript file, right-click, and choose **Copy Content to File**.

The extension will:

- Show a file picker listing all compatible files in your project
- Insert the selected code at the smart insertion point (configurable)
- Merge or skip duplicate import statements automatically

**Tip:** Use **Move Content to File** to cut and paste in one step.
