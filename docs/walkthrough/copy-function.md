# Copy Function

Place your cursor anywhere inside a TypeScript or JavaScript function, then right-click to open the context menu and select **Copy Function**.

The extension will:

- Detect the full function body using AST analysis
- Copy the function along with its import statements
- Place the result on your clipboard, ready to paste anywhere

**Supported function types:** regular functions, arrow functions, async functions, class methods, React components, and more.
